# frozen_string_literal: true

# the Gemfile containing dependencies to run tests should always be included in
# the root of a module. Adhere to Ruby conventions just like any other Ruby
# source file. Employ rubocop to enforce these rules.

source 'https://rubygems.org/' do
  gem 'awspec', '~> 1.0.0'
  gem 'kitchen-terraform', '~> 3.1'
  gem 'kitchen-verifier-awspec', '~> 0.1.1'
  gem 'rhcl', '~> 0.1.0'
  gem 'rubocop', '~> 0.52.1'
end
